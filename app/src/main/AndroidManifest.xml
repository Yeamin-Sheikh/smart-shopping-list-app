<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartShoppingList">

        <!-- Main Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Item Creation and Editor Activity -->
        <activity
            android:name=".AddEditItemActivity"
            android:label="@string/title_add_edit_item"
            android:parentActivityName=".MainActivity" />

        <!-- Budget and Spending Overview Activity -->
        <activity
            android:name=".BudgetOverviewActivity"
            android:label="@string/title_budget_overview"
            android:parentActivityName=".MainActivity" />

        <!-- Category Manager Activity -->
        <activity
            android:name=".CategoryManagerActivity"
            android:label="@string/title_category_manager"
            android:parentActivityName=".MainActivity" />

    </application>

</manifest>
